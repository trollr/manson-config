spring:
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/masonProvider
    username: masonProvider
    password: masonProvider
  jpa:
    database: POSTGRESQL
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate.cache.use_second_level_cache: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/